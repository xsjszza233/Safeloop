import Link from "next/link";
import { PageHeader } from "@/components/page-header";
import { StatusBadge } from "@/components/status-badge";
import { anomalies } from "@/data/mock-data";
const variants = { "草稿": "slate", "待确认": "amber", "已确认隐患": "blue", "一般异常": "green", "已驳回": "red" } as const;
export default function AnomaliesPage() { return <><div className="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><PageHeader title="异常管理" description="检查人员和设备责任人只能提交异常；EHS 管理员确认后才形成隐患。" /><Link href="/anomalies/new" className="mb-6 rounded-lg bg-[#167864] px-4 py-2.5 text-center text-sm font-semibold text-white">上报异常</Link></div><div className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="bg-slate-50 text-xs text-slate-500"><tr><th className="px-5 py-3">异常编号</th><th className="px-5 py-3">异常描述</th><th className="px-5 py-3">关联设备</th><th className="px-5 py-3">上报人</th><th className="px-5 py-3">状态</th><th className="px-5 py-3">操作</th></tr></thead><tbody>{anomalies.map((item) => <tr key={item.id} className="border-t border-slate-100"><td className="px-5 py-4 font-semibold text-slate-700">{item.id}</td><td className="px-5 py-4">{item.title}</td><td className="px-5 py-4">{item.device}</td><td className="px-5 py-4">{item.reporter}</td><td className="px-5 py-4"><StatusBadge variant={variants[item.status]}>{item.status}</StatusBadge></td><td className="px-5 py-4">{item.status === "待确认" ? <Link className="font-medium text-[#167864]" href={`/anomalies/${item.id}/confirm`}>确认异常</Link> : "—"}</td></tr>)}</tbody></table></div></div></> }
