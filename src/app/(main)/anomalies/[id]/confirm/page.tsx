import { notFound } from "next/navigation";
import { PageHeader } from "@/components/page-header";
import { anomalies } from "@/data/mock-data";
import { BackButton } from "@/components/back-button";
export default async function ConfirmAnomalyPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const anomaly = anomalies.find((item) => item.id === id); if (!anomaly) notFound(); return <><BackButton fallbackHref="/anomalies" label="返回异常列表" /><PageHeader title="异常确认" description="仅 EHS 管理员可确认异常是否形成隐患。" /><div className="mx-auto max-w-2xl rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-xs text-slate-400">{anomaly.id} · {anomaly.reportedAt}</p><h2 className="mt-2 text-xl font-bold">{anomaly.title}</h2><dl className="mt-6 grid grid-cols-2 gap-5 text-sm"><div><dt className="text-slate-400">关联设备</dt><dd className="mt-1 font-medium">{anomaly.device}</dd></div><div><dt className="text-slate-400">上报人</dt><dd className="mt-1 font-medium">{anomaly.reporter}</dd></div></dl><div className="mt-6 rounded-lg bg-amber-50 p-4 text-sm text-amber-700">演示操作：确认后才会形成隐患，并由 EHS 管理员设定风险等级、整改责任人与整改期限。当前不写入真实数据。</div><div className="mt-5 flex flex-wrap gap-3"><button className="rounded-lg bg-[#167864] px-4 py-2.5 text-sm font-semibold text-white">确认形成隐患</button><button className="rounded-lg border border-slate-200 px-4 py-2.5 text-sm font-semibold">标记一般异常</button><button className="rounded-lg border border-rose-200 px-4 py-2.5 text-sm font-semibold text-rose-700">驳回</button></div></div></> }
