import { notFound } from "next/navigation";
import { PageHeader } from "@/components/page-header";
import { StatusBadge } from "@/components/status-badge";
import { devices } from "@/data/mock-data";
import { BackButton } from "@/components/back-button";
export default async function DeviceDetailPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const device = devices.find((item) => item.id === id); if (!device) notFound(); return <><BackButton fallbackHref="/devices" label="返回设备台账" /><PageHeader title={device.name} description={`${device.id} · 设备详情与历史记录`} action="编辑基础信息" /><div className="grid gap-6 lg:grid-cols-3"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm lg:col-span-2"><h2 className="font-bold">基础信息</h2><dl className="mt-5 grid grid-cols-2 gap-5 text-sm sm:grid-cols-3"><div><dt className="text-slate-400">设备编号</dt><dd className="mt-1 font-medium">{device.code}</dd></div><div><dt className="text-slate-400">设备类别</dt><dd className="mt-1 font-medium">{device.category}</dd></div><div><dt className="text-slate-400">责任人</dt><dd className="mt-1 font-medium">{device.owner}</dd></div><div><dt className="text-slate-400">所在区域</dt><dd className="mt-1 font-medium">{device.location}</dd></div><div><dt className="text-slate-400">设备状态</dt><dd className="mt-1"><StatusBadge variant={device.status === "正常" ? "green" : "amber"}>{device.status}</StatusBadge></dd></div></dl></section><section className="rounded-xl bg-[#167864] p-5 text-white shadow-sm"><p className="text-sm text-emerald-100">一物一码</p><div className="mt-4 grid h-28 w-28 place-items-center rounded-lg bg-white text-4xl text-[#167864]">▦</div><p className="mt-4 text-sm">二维码能力将在后续版本接入。</p></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm lg:col-span-3"><h2 className="font-bold">点检记录、异常与隐患历史</h2><p className="mt-3 text-sm text-slate-500">当前为界面骨架，后续将在接入业务数据后展示此设备的完整可追溯记录。</p></section></div></> }
