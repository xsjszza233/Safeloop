import Link from "next/link";
import { PageHeader } from "@/components/page-header";
import { StatusBadge } from "@/components/status-badge";
import { dashboardStats, hazards, inspectionTasks } from "@/data/mock-data";
const tones: Record<string, string> = { emerald: "bg-emerald-50 text-emerald-700", amber: "bg-amber-50 text-amber-700", blue: "bg-blue-50 text-blue-700", violet: "bg-violet-50 text-violet-700" };
export default function DashboardPage() { return <><PageHeader title="首页看板" description="你好，陈小安。这里汇总当前设备点检与隐患闭环情况。" /><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{dashboardStats.map((stat) => <section key={stat.label} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><p className="text-sm font-medium text-slate-500">{stat.label}</p><span className={`grid h-8 w-8 place-items-center rounded-lg ${tones[stat.tone]}`}>●</span></div><p className="mt-4 text-3xl font-bold text-slate-900">{stat.value}</p><p className="mt-1 text-xs text-slate-400">{stat.hint}</p></section>)}</div><div className="mt-6 grid gap-6 xl:grid-cols-5"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm xl:col-span-3"><div className="mb-4 flex items-center justify-between"><h2 className="font-bold text-slate-800">近期检查任务</h2><Link href="/inspection-tasks" className="text-sm font-medium text-[#167864]">查看全部 →</Link></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b border-slate-100 text-xs text-slate-400"><tr><th className="pb-3 font-medium">任务编号</th><th className="pb-3 font-medium">设备</th><th className="pb-3 font-medium">截止时间</th><th className="pb-3 font-medium">状态</th></tr></thead><tbody>{inspectionTasks.slice(0, 4).map((task) => <tr key={task.id} className="border-b border-slate-50"><td className="py-3 font-medium text-slate-600">{task.id}</td><td className="py-3 text-slate-600">{task.device}</td><td className="py-3 text-slate-500">{task.deadline}</td><td className="py-3"><StatusBadge variant={task.timeTag === "已逾期" ? "red" : task.timeTag === "即将到期" ? "amber" : "blue"}>{task.timeTag}</StatusBadge></td></tr>)}</tbody></table></div></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm xl:col-span-2"><div className="mb-4 flex items-center justify-between"><h2 className="font-bold text-slate-800">待关注隐患</h2><Link href="/hazards" className="text-sm font-medium text-[#167864]">查看全部 →</Link></div><div className="space-y-3">{hazards.slice(0, 4).map((hazard) => <Link href={`/hazards/${hazard.id}`} key={hazard.id} className="block rounded-lg border border-slate-100 p-3 transition hover:border-emerald-200 hover:bg-emerald-50/30"><div className="flex items-center justify-between gap-2"><p className="truncate text-sm font-medium text-slate-700">{hazard.title}</p><StatusBadge variant={hazard.level === "高" ? "red" : hazard.level === "中" ? "amber" : "blue"}>{hazard.level}风险</StatusBadge></div><p className="mt-1 text-xs text-slate-400">{hazard.device} · 截止 {hazard.deadline}</p></Link>)}</div></section></div></> }
