import { notFound } from "next/navigation";
import { PageHeader } from "@/components/page-header";
import { StatusBadge } from "@/components/status-badge";
import { hazards } from "@/data/mock-data";
import { BackButton } from "@/components/back-button";
export default async function HazardDetailPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const hazard = hazards.find((item) => item.id === id); if (!hazard) notFound(); return <><BackButton fallbackHref="/hazards" label="返回隐患列表" /><PageHeader title="隐患详情" description={`${hazard.id} · 正式整改闭环过程`} action="查看整改记录" /><div className="grid gap-6 lg:grid-cols-3"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm lg:col-span-2"><div className="flex justify-between gap-4"><div><p className="text-sm text-slate-400">隐患描述</p><h2 className="mt-1 text-xl font-bold">{hazard.title}</h2></div><StatusBadge variant={hazard.level === "高" ? "red" : "amber"}>{hazard.level}风险</StatusBadge></div><div className="mt-6 grid grid-cols-2 gap-5 text-sm"><div><p className="text-slate-400">关联设备</p><p className="mt-1 font-medium">{hazard.device}</p></div><div><p className="text-slate-400">整改责任人</p><p className="mt-1 font-medium">{hazard.owner}</p></div><div><p className="text-slate-400">整改期限</p><p className="mt-1 font-medium">{hazard.deadline}</p></div><div><p className="text-slate-400">当前状态</p><p className="mt-1 font-medium">{hazard.status}</p></div></div></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold">处理流程</h2><ol className="mt-5 space-y-4 border-l border-emerald-200 pl-4 text-sm text-slate-500"><li>异常上报</li><li>确认隐患</li><li className="font-semibold text-[#167864]">{hazard.status}</li><li>复查关闭</li></ol></section></div></> }
